language: node_js

node_js:
  - "11"

cache: yarn

stages:
  - test

jobs:
  include:
    - stage: "test"
      name: "Check lint"
      script:
        - yarn run eslint ./
    - name: "Running tests"
      script:
        - yarn test --no-watch 
