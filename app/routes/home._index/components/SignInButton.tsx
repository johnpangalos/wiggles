export function SignInButton() {
  return (
    <button
      onClick={() => {
        google;
      }}
      className="px-3 py-2 pointer-cursor bg-gray-300 text-black"
    >
      Click Me
    </button>
  );
}
